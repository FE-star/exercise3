language: node_js
node_js:
    - 8
install:
|
  npm install -g npm@latest
  npm --version
  npm install --registry http://registry.npmjs.org
  npm install -g yarn
  yarn install
script:
- npm run test