addons:
  chrome: stable
language: node_js
node_js:
  - "8.9.1"
before_install:
  - google-chrome-stable --headless --disable-gpu --remote-debugging-port=9222 http://localhost &
  - npm i -g npm@5.5.1
  
install:
  - npm install
  - npm install -g karma
script:
  - npm run test