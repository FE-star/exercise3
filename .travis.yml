language: node_js
node_js:
  - 8
sudo: required
addons:
  chrome: stable
script:
  - karma start --single-run
before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - npm install -g karma-cli