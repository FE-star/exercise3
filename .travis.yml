# 使用的语言
language: node_js

# 使用的nodejs版本
node_js:
  - "10"

# 仓库的分支
branches:
  only:
  - master

# 执行install
install:
 - npm install mocha karma-cli -g
 - cd node_modules\karma-jasmine
 - npm install
 - cd ../../
 - npm install

# 执行命令脚本
script:
 - npm test