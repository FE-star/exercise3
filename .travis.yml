sudo: required

addons:
  chrome: stable

language: node_js

node_js:
  - 10.15.0

cache:
  directories: node_modules

branches:
  only: master

before_script:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start